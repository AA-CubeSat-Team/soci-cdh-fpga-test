The 'software' folder is the root directory for the eclipse files, containing:

* NIOS_2, which contains all C related files, including our own .c and .h files.

* NIOS_2_bsp, which contains all other support files, including ones that
  makes the processor possible